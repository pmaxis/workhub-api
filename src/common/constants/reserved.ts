/** Reserved admin role — single user, hidden from admin panel. */
export const ADMIN_ROLE_SLUG = 'admin';

/** Reserved permission for full admin access — hidden from admin panel. */
export const MANAGE_ALL_PERMISSION_KEY = 'manage.all';
